1.	特征：
线性结构
数组的子集，即也是数组
使用链表也可以实现
只能从一端（队尾）添加元素，只能从另一端（队首）取出元素
先进先出（FIFO）

2.	实现：（接口）
enqueue（进队）
dequeue（出队）			//复杂度O（n）
getFront（得到队首元素）
getsize（得到元素数量）
isEmpty（是否为空 ）

循环队列（针对数组队列）		//出队复杂度O（1）
两个指针，指向队首与队尾（下个元素入队位置）
fornt == tail --> 队列为空
出队 fornt ++
（tail +1） % c  == fornt	--> 队列为满 （还剩一个空间）
