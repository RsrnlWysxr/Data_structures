图 = 节点 + 边
变量：
顶点个数 -- n
边树 -- n
bool -- 是否为有向图

表示方式：
邻接矩阵：表示稠密图
完全图（每个点都和其他点相连）
n*n的二维矩阵
vector<vector<bool>>
沿负对角线   对称

邻接表：表示稀疏图
vector<vector<int>>
缺点：平行边的存在，判断是否两个节点间是否存在边，时间复杂度为O(n)

操作：
遍历临边
深度优先：
复杂度：稀疏图：O(V + E )
稠密图：O(V^2)
用处：判别连通分量的数量
   连个节点是否相连（并查集思想，加入id数组）
   获得两点间的一条路径（遍历到每个节点，记录之前到达的代码，倒推出路径）
广度优先：
使用队列
复杂度：稀疏图：O(V + E )
稠密图：O(V^2)
用处：无权图最短路径

